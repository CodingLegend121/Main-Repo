function truck(x,y) {
  this.x = x;
  this.y = y;
  this.xdir = 0;
  this.ydir = 4;

  this.show = function() {
    image(img2,this.x,this.y,100,190)
  }

  this.move = function() {
    this.x = this.x + this.xdir;
    this.y = this.y + this.ydir;
  }

}
